<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="top.omooo.admin.chartsdemo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:hardwareAccelerated="false"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity"
            android:theme="@style/AppThemeNoBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ChartActivity"/>

        <activity android:name=".BesselCurveDemo"/>

        <activity android:name=".PieChartDemo"/>

        <activity android:name=".RadarMapDemo"/>

        <activity android:name=".WavesDemo"/>

        <activity android:name=".NativeEditDemo"/>

        <activity android:name=".ProgressDemo"/>
        <activity android:name=".ShowLyricDemo"/>
        <activity android:name=".DemoActivity"/>
        <activity android:name=".PocketActivity"/>
        <activity android:name=".MPPieChartActivity"/>
        <activity android:name=".MoveViewActivity"/>
        <activity android:name=".ScrollViewActivity"/>
        <activity android:name=".DrawBoardActivity"/>

        <activity android:name=".banner.BannerActivity"/>
    </application>

</manifest>